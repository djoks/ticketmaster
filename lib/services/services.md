# services/

Implements the service pattern. Services here handle app-wide business logic (e.g. network calls, notifications, or any standalone feature). Services can delegate data operations to the **repositories** if needed.
